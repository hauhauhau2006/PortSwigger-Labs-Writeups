# SQL Injection: Retrieving data from other tables

**Lab Description:** The lab explicitly states that the database has a table named `users` with columns `username` and `password`.

**Quite eassy, because lab provides everthings I need to solve. I just fill and get administrator account**
